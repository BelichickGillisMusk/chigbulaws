{
  "name": "chigbulaws",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "github:cloudflare-secrets": "bash scripts/set-github-cloudflare-secrets.sh"
  },
  "devDependencies": {
    "wrangler": "~4.86.0"
  }
}
